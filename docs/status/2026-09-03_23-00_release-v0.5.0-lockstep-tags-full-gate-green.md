# Release v0.5.0: Lockstep Tags ×3, Full Gate Green, Consumer Build Verified

**Session:** 2026-09-03, ~22:30–23:00 CEST · **Release commit:** `831bbfb` · **Base:** v0.4.0 (tagged 2026-09-03 00:36)

## Result

v0.5.0 shipped for all three modules in lockstep (`v0.5.0`, `static/v0.5.0`,
`datastartest/v0.5.0`, all annotated on `831bbfb`), pushed, proxied, and
released on GitHub. 63 commits since v0.4.0; bump rationale: new exported API
(Response method forms, `version` package, 3 datastartest helpers, static
hardening) → MINOR; one deprecation (`datastar.DatastarJSVersion`), no
breaking changes.

## What shipped

- Response method forms (`resp.ErrorResponse`, `resp.ErrorResponseFromError`,
  `resp.NotificationResponse`)
- `version` package (build-time `Version` var for binary consumers)
- datastartest: `RequireElementsOrdered`, `Diff`, `Snapshot`
- static: embedded JS v1.0.3 (opt-in CSP, canonical minified bundle 33.5 KB),
  `nosniff`, SHA-256 checksum pin, provenance comment
- Deprecation: `datastar.DatastarJSVersion` (use `Version()` / `static.Version`)

## Verification ledger

| Gate                                          | Outcome                                                              |
| --------------------------------------------- | -------------------------------------------------------------------- |
| Race suite ×3 modules (`-count=1`)            | ok ×6 packages                                                       |
| `go vet` / golangci-lint v2.12.2 (CI parity)  | clean / 0 issues                                                     |
| `nix flake check`                             | all checks passed (after one `datastartestVendorHash` refresh)       |
| `go work sync` idempotency                    | go.work unchanged                                                    |
| Isolation (`GOWORK=off`) build+test ×3        | OK ×3                                                                |
| `GOWORK=off go mod tidy -diff` ×3 (pre+post)  | clean ×3                                                             |
| `go mod verify` ×3                            | all modules verified ×3                                              |
| Replace audit (`replace.*=>/`)                | nothing found (relative only)                                        |
| `nix run .#govulncheck`                       | No vulnerabilities found                                             |
| CI on the exact tagged commit (`831bbfb`)     | green (all workflows incl. nix) before tagging                       |
| Proxy `go list -m -versions` ×3               | v0.5.0 listed ×3                                                     |
| `go get` ×3 in clean scratch module           | resolved with checksum-DB verification (no `GONOSUMDB` needed)       |
| Consumer compile against `@v0.5.0`            | builds (imports `datastar` + `static`)                              |
| GitHub Releases ×3                            | root `--latest`, submodules `--latest=false` (v0.4.0 audit pattern)  |
| pkg.go.dev                                    | crawl lag on all three at session end (external; see lesson d4 of the release-superb audit — re-check later, proxy side proven correct) |

## Notes

- The auto-commit daemon committed the release prep (CHANGELOG promote,
  lockstep requires, vendorHash) before I could stage manually; commits
  verified content-wise and pushed deliberately, CI awaited on the exact
  commit before tagging (the v0.3.0 tag lesson).
- `datastartestVendorHash` moved exactly once on the sibling-require bump and
  converged on the first paste — consistent with the ADR 004 minimal-fileset
  fix ("requires move it, metadata edits don't").
- CHANGELOG `[Unreleased]` left as bare header per house style; compare links
  updated (`[0.5.0]`, `[Unreleased] → v0.5.0...HEAD`).
